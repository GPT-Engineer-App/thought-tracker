import { Box, Heading, Text } from '@chakra-ui/react';

const About = () => (
  <Box p={4}>
    <Heading>About Us</Heading>
    <Text mt={2}>Learn more about our note-taking app and the team behind it.</Text>
  </Box>
);

export default About;